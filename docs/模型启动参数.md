### qwen25-1.5b

## 启动接口

```text
POST /v2/api/aione/model/run
```

请求需携带外部 API 的 Bearer Token。请求体中的 `id` 是必填的模型唯一标识，
无需再将它拼接到 URL 中。接口会创建 VLLM 模型 App，并返回：

```json
{
  "status": 200,
  "data": {
    "name": "mod-epc8fd1hzpadso7k6lw20w3312",
    "code": "deepseek4.0",
    "profile": "VLLM",
    "url": "http://..."
  }
}
```

调用方给出的 `param` 按行拆分为 VLLM 参数；服务会自动补充模型文件路径、监听地址
`0.0.0.0` 和端口（默认 `8000`）。

--served-model-name
qwen25-15b
--max-num-seqs
16
--max-model-len
8192
--enforce-eager

### 仓库地址

http://gitea.ops.fzyun.io/aione/Qwen2.5-1.5B-Instruct.git

### 镜像地址

docker.ops.fzyun.io:5000/vllm/vllm-openai:latest
